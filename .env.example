# Dealer Page Optimizer — Environment Variables

# Optional: Set a custom User-Agent for server-side page fetching
# FETCH_USER_AGENT="Mozilla/5.0 (compatible; DealerPageOptimizer/1.0)"

# Optional: Fetch timeout in milliseconds (default: 12000)
# FETCH_TIMEOUT_MS=12000
